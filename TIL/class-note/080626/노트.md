
# 05.
## venv
 검증용으로 사용 하는 환경.,
 프로젝트 마다 독립된 패키지 공간이다.
  로봇 에서는 사용 하지 않는다.(커널에서 자동으로 venv를 키는것은 좋지 않다,)

리스트는 가변
튜플은 불변
dict 는 

### 시간복잡도
배열을 루프 돌렸을 때 시간이 얼마나 걸리나.

O(1) 이 무슨 뜻?

$O(1)$
$O(n)$
$O(n^2)$

worstcase = $O(n^2)$
averagecase = $O(n^2)$
bestcase = $O(n)$

자료 정렬 알고리즘이 중요하다 - bubble  sort알고리즘이 빠름

`%timeit x in my_list` 로 노트북에서 시간 복잡도를 측정할 수 있다.


객체지향을 쓰는 이유
이벤트가 발생 할 때마다(필요 할때마다)객체를 참조하여 코드를 해석하고 실행 하기 위해

매직 메서드
-  내부 적으로 자동 호출 되는 함수

캡슐화


# 06.

### Numpy
벡터화 연산이 구조상 list보다 훨씬 빠름
반복문을 쓰지 않고 한번에 연산 하라 (벡터화)

#### 왜 빠른가?
메모리 구조가 다름!
list - 원소마다 포인터 추적 (포인터의 목록),원소마다 타입 확인
numpy - 같은 크기( dtype)  을 고정 크기 로 연속된 메모리에 저장함.

axis 는 어느 방향으로 연산할지.
axis 개념 좀더 설명해줘

dtype을 잘 설정해서 오버플로우를 조심하자.

슬라이스는 데이터를 복사 하지 않고 **보는** 기능이다

반복문 보다 코드를 벡터화로 바꾸는 사고 과정을 거치자.



# 오늘의 퀘스트

오늘 배운 내용을 5문항으로 복습해요. 답하면 바로 정답과 해설을 볼 수 있어요.

에이타니

1번 문항

Python 프로젝트에서 venv(가상환경)를 사용하는 가장 큰 이유로 적절한 것은?

B. 프로젝트별 패키지 버전 충돌을 막고 시스템 Python을 보호하려고

에이타니

정답이에요

venv는 프로젝트마다 독립된 패키지 공간을 만들어 버전 충돌을 막고, 시스템 Python(OS·ROS2가 쓰는)을 pip install로부터 보호합니다. venv는 실행 속도나 메모리 사용량과는 무관하며, 타입 검사 강제는 mypy 같은 별도 도구의 역할입니다.

에이타니

2번 문항

Jupyter 노트북에서 import numpy가 ImportError로 실패할 때, 가장 먼저 확인해야 할 것과 그 확인 방법(코드)을 쓰세요.

numpy -v

에이타니

오답이에요

ipykernel install을 빠뜨리면 노트북이 시스템 파이썬으로 돌아 분명히 설치했는데 ImportError가 납니다. sys.executable을 출력해 현재 커널이 원하는 가상환경의 파이썬인지 확인하는 것이 첫 단계입니다.

에이타니

3번 문항

다음 코드를 실행했을 때 두 print문의 출력으로 옳은 것은? def add(v, log=[]): log.append(v) return log print(add(1)); print(add(2))

D. TypeError 발생

에이타니

오답이에요

기본값 []는 함수 정의 시점에 단 한 번 만들어져 모든 호출이 이를 공유합니다. add(1)은 [1]을 출력하고, add(2)는 같은 리스트에 2를 추가해 [1, 2]를 출력합니다. 매 호출마다 새 리스트가 생긴다고 생각하면 오답을 고르게 됩니다.

에이타니

4번 문항

데이터가 100만 개로 늘어나도 dict/set의 포함 검사(in)가 list보다 훨씬 빠른 이유는 무엇인가요?

A. 해시 테이블을 사용해 키의 위치를 즉시 계산하기 때문에(O(1))

에이타니

정답이에요

dict/set은 해시 함수로 항목이 저장될 위치를 바로 계산해 데이터 크기와 무관하게 O(1)에 조회합니다. 반면 list의 in 검사는 처음부터 끝까지 순차 탐색(O(n))해야 합니다. 정렬·이분탐색이나 압축 저장은 교안에서 다룬 원리가 아닙니다.

에이타니

5번 문항

부모 클래스에서 정의한 메서드(예: read)를 자식 클래스가 자기 방식으로 다시 정의하는 것을 무엇이라고 하나요?

매직 매서드

에이타니

오답이에요

Sensor의 read를 Lidar, Imu가 각자의 방식으로 다시 정의하는 것이 오버라이드(재정의)입니다. 이렇게 하면 사용하는 쪽은 타입을 몰라도 s.read()로 동일하게 호출할 수 있는데, 이것이 다형성입니다.