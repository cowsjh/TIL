lidar 센서는 극좌표(각도) 로 표현 되기 때문에 극좌표를 직교 과표로 변환 시키는 기능이 필요하다.
- 레이저를 쏘는 부분과 반사값을 받는 파트를 가지고 있고 회전 하면서 360값을 받는다.

### ssh 키 생성
github 를 위한 키생성
필요한 이유는?
```bash
ssh-keygen -t rsa #ssh 키 생성
cat 키 경로 # 키 확인
```



# 오늘의 퀘스트

오늘 배운 내용을 5문항으로 복습해요. 답하면 바로 정답과 해설을 볼 수 있어요.

에이타니

1번 문항

30Hz로 도는 인지 루프 안에서 누적된 ID 목록에 대해 `if x in ids:` 검사를 반복할 때, 자료구조 선택에 따른 성능 차이를 가장 올바르게 설명한 것은?

B. list는 O(n), dict/set은 O(1)이라 데이터가 많아질수록 list의 성능이 급격히 나빠진다

에이타니

정답이에요

list의 포함 검사는 전체를 순회해야 하므로 O(n)이고, dict/set은 해시 테이블로 즉시 위치를 계산하므로 O(1)입니다. 원소가 100만 개면 약 2,500배 차이가 나며, 이는 30Hz 인지 루프에서 시간이 지날수록 마감을 놓치는 성능 버그가 됩니다. 따라서 소속 검사가 반복되는 루프에서는 set을 쓰는 것이 원칙입니다.

에이타니

2번 문항

Jupyter 노트북에서 `import numpy`가 ImportError로 실패할 때, 가장 먼저 확인해야 할 것과 그 확인 방법을 한 문장으로 쓰세요.

numpy가 설치 되었는지 , kenel리셋 하고 restart 하고 확인 한번 더

에이타니

오답이에요

ipykernel install 단계를 빠뜨리면 노트북이 시스템 파이썬으로 돌아, 분명히 설치했는데 ImportError가 납니다. 셀에서 `import sys; print(sys.executable)`을 실행하면 커널이 실제로 어느 파이썬 인터프리터를 쓰는지 바로 확인할 수 있습니다.

에이타니

3번 문항

Jupyter Notebook에서 셀을 위아래로 오가며 실행한 뒤, 노트북을 제출·공유하기 전에 반드시 해야 하는 습관으로 가장 적절한 것은?

B. Kernel → Restart Kernel and Run All Cells로 위에서 아래까지 한 번에 통과하는지 확인한다

에이타니

정답이에요

셀은 독립적이지 않고 같은 커널 메모리를 이어서 건드리는 조각이므로, 위아래로 오가며 실행하면 화면 순서와 실제 실행 이력이 어긋날 수 있습니다. Restart Kernel and Run All Cells는 커널을 초기화하고 위에서 아래로 순서대로 재실행하여 결과의 신뢰성을 확인하는 표준 습관입니다.

에이타니

4번 문항

내 클래스의 인스턴스를 `print()`로 출력했을 때 읽기 좋은 문자열이 나오도록 정의하는 매직 메서드의 이름은 무엇인가요?

__str__

에이타니

정답이에요

`__repr__`을 정의하면 `print(v)`나 객체를 그대로 출력했을 때 읽기 좋은 표현이 나옵니다. 교안의 Vector2 예시에서도 `__repr__`을 정의해 `Vector2(4.00, 3.00)`처럼 출력되도록 했습니다.

에이타니

5번 문항

Python의 타입 힌트에 대한 설명으로 옳은 것은?

B. 타입 힌트는 문서화, 도구 지원(자동완성), mypy 같은 정적 검사 도구의 기반이 되지만 실행을 강제하지는 않는다

에이타니

정답이에요

타입 힌트는 강제가 아니며 인터프리터는 힌트를 무시하고 실행합니다. 대신 문서화, 편집기의 자동완성·경고 같은 도구 지원, mypy 같은 정적 검사 도구로 실행 전에 오류를 잡는 데 활용됩니다. 실행 속도 향상과는 무관합니다.